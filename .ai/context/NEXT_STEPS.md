# NEXT_STEPS

Last Updated: 2026-05-18

## One-Line Summary
- 首页删除入口与续播核心口径已落地，当前主线是外部验收闭环与证据回写。

## Current Priority Modules
- `M-S4-CONTROL-002`（最高优先）: 完成浮窗 UI 热修后的车机复测，收口 `T-S4-CORE-026C-HF-20260429`。
- `M-S4-RESUME-003`（并行）: 完成 `T-S4-RESUME-020E` 文档与清单回写。
- `M-S4-OBS-006`（并行）: 在线查询验收并回传证据。
- `M-S4-UPD-007`（并行）: 完成 CI/实机更新链路闭环验证。

## Recommended Execution Mode
- 当前推荐: 模块推进（`ai-module-execution`）。
- 原因: 现在任务大多是“同一链路连续验证”，分散成微任务会反复切上下文。

## What To Start First (按顺序)
1. `T-S4-CORE-026C-HF-20260429` 复测（浮窗 UI 修正后）
- 验证点: 歌名字号是否明显增大、关闭按钮是否在右上角且易点击。
- 为什么先做: 这是当前唯一新增现场问题，修正后应优先回归确认。

2. `T-S4-RESUME-020E` 续播策略验收清单回写
- 验证点: 完成策略分层（恢复列表/进度/自动起播）、默认值与验收清单更新。
- 为什么现在做: 代码已切到“仅索引恢复 + 自动鉴权续播”，需先对齐文档口径再进回归。

3. `T-S4-OBS-035/036/037` PostHog 查询验收
- 验证点: 直接在 PostHog 看车机 session 事件流（有无、完整性、字段正确性、敏感字段门禁）。
- 为什么现在做: 用户已确认“先查已上报事件”，只在异常时回查客户端。

4. `T-S4-OBS-038` 在线查询与 AI 导出执行
- 验证点: 按 `docs/POSTHOG_QUERY_EXPORT_TEMPLATE.md` 完成 1 条 session 时间线导出与聚合查询结果。
- 为什么现在做: 模板已就绪，当前仅差在线执行证据。

5. `T-S4-UPD-044` 更新链路闭环验收
- 验证点: 检查更新 -> 下载 -> 安装触发 -> 事件可查。
- 为什么现在做: `040/041/042/043` 已完成，代码侧已收口，当前只差最后闭环验证。

6. 车机窗口到位后执行 `T-S4-REG-022` + `T-S4-VAL-033`
- 验证点: 全量回归 PASS/FAIL/Blocker + context 回写收口。

## Main Blockers
- 车机测试窗口不可控（直接阻塞 `T-S4-REG-022`）。
- 当前仓库无 `gradlew`，需使用系统 `gradle` 执行本地编译验证。
- 当前环境无 `adb`，本地无法直接抓设备日志。
- 低版本系统 TLS 兼容问题，可能影响更新检测稳定性。

## Need Confirmation
- 续播目标口径: 当前已确认“仅索引自动续播 + 会话缺失自动鉴权”，是否需要增加失败提示文案分级。
- PostHog 环境口径: 是否需要额外 `dev` 项目隔离测试数据。
- 更新策略: 检测周期（默认 24h）与是否仅 Wi-Fi。
- 镜像策略: 当前优先级是否固定，是否需要设置页可配置。
- 延后需求口径: 长标题滚动、音效优化细节。

## Explicitly Deferred
- `T-S4-UI-023` 长标题滚动异常修复
- `T-S4-AUDIO-025` 均衡器/音效优化
