# PLAN

Last Updated: 2026-05-12

## Current Stage
- Stage Name: S4 本地收口（车机验收前）
- Scope Source: `.ai/context/SCOPE.md`（2026-05-06）
- Stage Goal: 在不扩新需求前提下，先收口“首页删除入口未达预期”与“续播口径重规划”两条主线，形成可执行验收入口。

## This Stage In Plain Words
- 这阶段不是继续堆功能，而是把现有能力跑通、说清楚、留证据。
- 重点是 5 件事：后台控制稳定、删除入口可见可用、恢复逻辑口径一致、更新链路可用、验收清单可执行。
- 车机或 CI 才能完成的事情，明确标记为阻塞，不假装本地能做完。

## In Scope
- 后台控制链路收口：Service/Activity 职责边界清晰，命令成功失败可追踪。
- 删除入口收口：首页推荐列表与队列页删除入口位置、可见性、双确认链路一致。
- 浮窗与通知策略验证：播放/暂停显示、手动关闭后“进应用再切出”再显示。
- 熄火/休眠恢复闭环：当前“无自动续播”口径的行为稳定，续播能力的后续形态先规划再实现。
- PostHog 关键事件链路：用于排障和验收证据，不替代全量日志。
- 更新链路闭环：自动检查、手动检查、下载、触发安装、关键事件记录。
- API17 验收清单与证据回写流程。

## Out of Scope
- 新需求扩展（新协议、新后端、MediaSession 主链路切换、大改 UI）。
- 静默安装、自建 OTA、差分更新。
- 未确认需求直接进入 Ready（系统首页卡片入口、歌词失败策略等）。

## Major Work Blocks

### W1 核心链路收口
- 要做什么: 把“谁负责执行命令、谁负责记录状态”说清楚并固化。
- 做完算什么: 前台/后台不会出现“看起来执行了其实没执行”。

### W2 后台控制面一致性
- 要做什么: 把媒体键、通知、浮窗这三条控制入口行为对齐。
- 做完算什么: 三条入口的上一曲/暂停/下一曲结果一致。

### W3 恢复链路闭环
- 要做什么: 明确“恢复列表/恢复进度/自动起播”三者边界，并补齐验证清单。
- 做完算什么: 当前口径不歧义，后续续播功能是否回归有清晰决策入口。

### W3A 删除入口可用性闭环
- 要做什么: 重新规划首页删除按钮位置与样式，保证首屏可见、点击命中稳定。
- 做完算什么: 首页与队列/库页删除入口一致，双确认与删除反馈链路可复测。

### W4 观测链路闭环（PostHog）
- 要做什么: 关键节点有结构化事件，能定位失败点。
- 做完算什么: 至少能查到一条完整 session 事件流。

### W5 更新链路闭环
- 要做什么: 自动/手动检测更新，下载失败可回退，下载后能触发安装。
- 做完算什么: 更新全链路有日志和事件，可复盘。

### W6 实机回归与证据回写
- 要做什么: 用 S4 清单做实机回归，把结论写回 context。
- 做完算什么: PASS/FAIL/Blocker 有证据，下一步任务清楚。

## Recommended Order
1. 先完成 `M-S4-UX-005` 的规则规划（`T-S4-UI-024B`）。
2. 基于规则复开 `T-S4-UI-024A`，修复首页删除入口可见性与点击命中。
3. 并行完成 `M-S4-RESUME-003` 的 `T-S4-RESUME-020D/020E`（先定口径，再谈功能回归）。
4. 再推进 `M-S4-CONTROL-002` 与 `M-S4-CORE-001` 的车机验证收口。
5. 并行完成 `M-S4-OBS-006`、`M-S4-UPD-007` 外部验收。
6. 执行 `T-S4-REG-022` 并完成 `T-S4-VAL-033` 回写收口。

## Dependency Graph
- `M-S4-CORE-001 -> M-S4-CONTROL-002 -> M-S4-RESUME-003 -> M-S4-VALID-004`
- `M-S4-OBS-006` 可并行，但 `REG-022` 前需达到可用状态。
- `M-S4-UPD-007` 可并行，但 `UPD-044` 需 CI/实机验收。

## Risks & Assumptions
- 风险: 当前环境没有 `gradle/gradlew`，本地不能完成编译验证。
- 风险: 车机测试窗口不可控，关键任务会被动排队。
- 风险: 低版本系统 TLS 兼容问题可能影响更新检测。
- 假设: 接受前台服务常驻通知。
- 假设: 目标设备允许浮窗权限。

## Stage Milestones
- M1: 核心命令链路收口，日志可定位。
- M2: 三条后台控制入口一致可用。
- M3: 续播口径（当前关闭 + 后续选项）清晰且可执行。
- M4: PostHog 关键事件可查。
- M5: 更新链路“检查->下载->安装触发”跑通。
- M6: 实机回归证据回填完成。
