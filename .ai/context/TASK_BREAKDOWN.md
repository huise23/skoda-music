# TASK_BREAKDOWN

Last Updated: 2026-06-08

## Active Stage: S5 纠偏 - Kugou Pure Source Playback, Queue/Radio Parity & MainActivity Split

## Planning Snapshot
- 用户新增确认：
  - 电台类型播放不是普通歌曲队列；同一电台持续播放，下一曲由电台内部切换。
  - `MainActivity.kt` 6k 多行问题不能只记录，当前需要优化拆分。
  - 酷狗队列必须参考 `.NET`，不能沿用 Emby 队列。
  - 默认酷狗模式不能叠加 Emby 播放。
  - DSP 播放按钮不能持续红色，红色应只代表真实 fail-open/bypass/error。
  - 用户已纠正：不能把“单 Activity 外壳”作为长期硬约束；项目继续变大时允许适当拆 Activity / 页面壳 / Fragment / Binder，目标是避免单文件行数过多。
- 当前计划改为先拆 `MainActivity`，再做酷狗 API/播放/队列/电台纠偏。
- Bootstrap 后新增工程红线已纳入规划：
  - `minSdk = 17` 是不可突破红线。
  - `KugouMusic.NET/` 是只读第三方参考项目，忽略扫描且不修改。
  - `MainActivity.kt` 约 6213 行，已超过 entry red-line；当前 Ready 必须优先压降入口文件，不允许继续添加大块职责。
- 2026-06-04 追加规划:
  - 手机环境点击刷新二维码 1-2 秒后崩溃，API17 车机实机回归暂缓。
  - 后续新增功能必须补足 PostHog/runtime/logcat 诊断日志，且敏感信息必须过滤。
  - 当前 Ready 转为 `T-S5-KG-119` QR refresh crash hotfix；观测补齐任务 `T-S5-OBS-120` 紧随其后。
- 2026-06-07 追加规划:
  - 车机登录酷狗成功后未自动加载各项列表。
  - 用户确认方案 B：登录成功直接拉默认页；其它页进入时懒加载；失败提示并可手动拉；运行中 token 失效不清内容，弹窗登录，登录后隐藏并继续当前页面。
  - 新增模块 `M-S5-KG-037`；`T-S5-KG-124/125/126/127` 已在本轮本地完成，等待实机验证。
- 2026-06-07 追加规划 2:
  - 用户纠正：首页中间播放块不改推荐；左侧新增“每日推荐”按钮。
  - 首次进入首页加载每日推荐并播放第一首；每日推荐不提供刷新按钮。
  - 酷狗模式隐藏旧 Emby 队列按钮。
  - 首页右侧展示当前播放列表/队列；所有队列自动滚动到当前歌曲。
  - Radio 队列展示 current/upcoming/history。
  - Radio/Scene 用缩略图网格；Scene tab 不横向滚动，默认两到三排，多余展开/收缩，点击后收缩。
  - Scene 来源已核对 `.NET`：`SceneClient` / `RawMediaCatalogApi` scene list/audio/module/music raw API。
- 2026-06-07 追加规划 4:
  - 本地功能链已完成到 VIP/权限提示，下一阶段转为“设备验证闭环 + MainActivity 红线拆分续作”。
  - 新增 `M-S5-VAL-041` 与 `M-S5-MAIN-042`。
  - 设备窗口可用时优先执行 `T-S5-VAL-137`；设备窗口不可用时已完成 `T-S5-MAIN-138/139`，继续开发前需重新规划下一批拆分。
- 2026-06-08 追加规划:
  - 首页首次进入加载并播放每日推荐，但用户手动切到其它播放列表后不得再被每日推荐抢播。
  - 左侧每日推荐入口只展示当日推荐列表，列表点击才播放。
  - 首页右侧当前队列未跟随下一曲，需以“当前播放项变化”为统一同步源修复。
  - 首页歌词需要恢复，直接从酷狗查歌词；播放列表 tab 10s 未操作自动切歌词，空播放不处理。
  - 播放块删除按钮前增加点赞按钮，复用其它页面点赞逻辑。
  - `.NET` UI 没有独立 Scene 页面，发现页改为一级 tab + 二级 tab + 歌单网格；去掉标题、二级说明和刷新按钮，一二级切换自动获取歌单。
  - 新增 `M-S5-HOMEUX-043`、`M-S5-DISCOVER-044`、`M-S5-DSP-045`。

## T-S5-HOME-141
- Task ID: `T-S5-HOME-141`
- Module ID: `M-S5-HOMEUX-043`
- Status: Ready
- Title: 每日推荐启动自动播放与左侧入口列表展示分离
- Goal: 保留首次进入首页加载并播放每日推荐第一首，同时把左侧“每日推荐”入口改为只展示列表，列表点击才播放，并在用户手动切换其它播放列表后停止每日推荐自动抢播。
- Why: 用户明确区分了“启动自动播放”和“左侧入口行为”；当前入口点击即播放会误触发，且自动播放不能覆盖用户之后选择的列表。
- Responsibility Boundary:
  - `DailyRecommendCoordinator`: auto-play guard、manual source switch lockout、pending login resume。
  - `KugouContentBinder`: 加载/展示每日推荐列表、列表点击回调。
  - `MainActivity`: 左侧按钮仅切换/委托展示，不直接调用 play-first。
- Dependencies:
  - `T-S5-HOME-128` 已完成但需纠偏。
  - `M-S5-KG-037` 登录恢复已存在。
- Inputs:
  - `app/src/main/java/com/skodamusic/app/ui/DailyRecommendCoordinator.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Expected Outputs:
  - 冷启动/缓存 session/登录成功首次首页自动加载并播放每日推荐第一首。
  - 左侧每日推荐入口显示每日推荐列表，不直接播放。
  - 用户从发现歌单、场景/发现分类、Radio、Emby 显式队列或其它列表手动播放后，每日推荐不再自动抢播。
  - 脱敏 runtime/PostHog 标记 auto-play skipped reason/manual-switch。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/DailyRecommendCoordinator.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（接线-only）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 自动播放和手动入口必须是两个明确方法，避免再次把入口点击误接到 play-first。
  - manual switch lockout 应由播放上下文变化触发，而不是仅靠页面切换。
- Comment Requirements:
  - 对“启动自动播放只执行一次，手动切源后失效”的 guard 写短注释。
- Done Criteria:
  - 首次进入首页自动播放每日推荐第一首。
  - 点击左侧每日推荐只显示列表，不播放。
  - 切到其它列表播放后，返回首页/登录恢复不会自动抢播每日推荐。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 设备/手测：启动自动播、入口不自动播、手动切源后不抢播。
- Risks:
  - 登录成功、VIP 流程、每日推荐加载可能并发触发；需利用 existing loading/pending guard。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-HOME-142
- Task ID: `T-S5-HOME-142`
- Module ID: `M-S5-HOMEUX-043`
- Status: Ready
- Title: 首页右侧当前队列跟随下一曲与自动滚动修复
- Goal: 以当前播放项变化作为统一同步源，确保首页右侧队列高亮和滚动跟随手动下一曲、自然下一曲、失败跳过、权限/VIP 跳过和 Radio advance。
- Why: 用户实机反馈右侧列表没有同步滚动，甚至没有跟随跳到下一曲；这是首页当前队列定义的核心验收。
- Responsibility Boundary:
  - `HomeQueuePanelBinder`: 渲染后定位当前行并滚动。
  - `MainActivity` / playback callbacks: 只在当前播放项变化时调用统一 `refreshHomeQueuePanel`/等价委托。
  - `KugouPlaybackQueueManager` / `KugouRadioSessionManager`: 只提供当前队列快照，不做 UI。
- Dependencies:
  - `T-S5-HOME-130` 已完成但需纠偏。
  - `T-S5-PLAY-111/112` queue/radio managers 已存在。
- Inputs:
  - `app/src/main/java/com/skodamusic/app/ui/HomeQueuePanelBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/playback/KugouPlaybackQueueManager.kt`
  - `app/src/main/java/com/skodamusic/app/playback/KugouRadioSessionManager.kt`
- Expected Outputs:
  - 单一 current item changed sync path。
  - 首页右侧列表在当前歌曲变化后更新选中态。
  - 自动滚动到当前歌曲，目标尽量居中或至少完整可见。
  - 如果当前播放项不在 UI 队列快照中，重建为当前播放会话快照而不是保留旧列表。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/HomeQueuePanelBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（接线-only）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 自动滚动只能在当前项变化或列表重绘后触发，避免高频 progress tick 触发滚动。
  - Radio 展示 history/current/upcoming 时 current 行必须成为滚动目标。
- Comment Requirements:
  - 对 `post {}` 布局后滚动保留 API17 timing 注释。
- Done Criteria:
  - 点击下一曲后右侧高亮移动并滚动。
  - 自然播放结束自动下一曲后同样更新。
  - 播放 URL 无权限/VIP、解码失败等自动跳过后同步到实际播放曲。
  - Radio next/current/upcoming 同步。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 设备/手测：每日推荐、歌单、Radio、Emby 显式队列各一次下一曲。
- Risks:
  - 当前播放变化路径分散在 Emby URL/cache、Kugou URL、Radio completion、失败恢复中，遗漏路径会导致只修一部分。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-HOME-143
- Task ID: `T-S5-HOME-143`
- Module ID: `M-S5-HOMEUX-043`
- Status: Ready
- Title: 首页歌词酷狗 direct 化与 10s 空闲切回歌词
- Goal: 恢复首页歌词 tab，按 `.NET` `LyricClient` / `RawLyricApi` 直接查询酷狗歌词，并在播放列表 tab 10s 无操作后自动切回歌词。
- Why: 用户反馈首页歌词被删了且需要；旧实现仍在 `MainActivity` 并走旧 LRC API，不符合“直接从酷狗查”的口径，也继续扩大入口文件。
- Responsibility Boundary:
  - `KugouLyricClient`: `lyrics.kugou.com/v1/search`、`/download`、KRC/LRC 解码和字段解析。
  - `HomeLyricsBinder`: 歌词缓存、当前行渲染、滚动、10s idle timer、播放列表用户操作重置计时。
  - `MainActivity`: 当前播放变化、position tick、tab 点击和 user interaction 事件委托。
- Dependencies:
  - `.NET` `RawLyricApi` / `LyricClient` / `KrcParser` 已核对。
  - 首页歌词 XML 仍存在。
- Inputs:
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/RawLyricApi.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/LyricClient.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Adapters/Lyrics/KrcParser.cs`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt` 当前 lyrics methods
- Expected Outputs:
  - 新增 `KugouLyricClient` 或等价 focused client。
  - 新增 `HomeLyricsBinder`，从 `MainActivity` 迁出歌词缓存/解析/渲染/滚动/idle 切换。
  - 切歌后加载当前酷狗歌词，失败显示“暂无歌词”并记录脱敏失败 stage。
  - 当前首页在队列 tab 且正在播放时，10s 无操作自动切回歌词；空播放不处理。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/kugou/KugouLyricClient.kt`
  - `app/src/main/java/com/skodamusic/app/ui/HomeLyricsBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（删除旧歌词逻辑/接线）
  - `app/src/main/res/values/strings.xml`（必要时）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectContentClient.kt`（歌词独立 client，避免继续膨胀）
- Architecture Notes:
  - KRC 解码可按 `.NET` `KgCrypto.DecodeLyrics` 移植必要逻辑；如果真实响应为 LRC/base64，则走 LRC 解码分支。
  - 歌词请求失败不得阻塞播放。
- Comment Requirements:
  - 对 KRC decode key/format 分支和 10s idle timer lifecycle 加短注释。
- Done Criteria:
  - 首页歌词 tab 可见且能显示酷狗歌词。
  - 切歌会刷新歌词和当前行滚动。
  - 队列 tab 10s 无操作自动切歌词，空播放不切。
  - `MainActivity` 中旧 LRC API 请求/解析主体被移出。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
  - 真实账号/设备验证歌词 search/download 成功与失败提示。
- Risks:
  - KRC 解码/压缩处理和 Java/Kotlin 字节处理容易出错；优先按 `.NET` 实现最小移植并保留失败诊断。
- Size: L
- Execution Mode: Module
- Minimal Loop: No

## T-S5-HOME-144
- Task ID: `T-S5-HOME-144`
- Module ID: `M-S5-HOMEUX-043`
- Status: Ready
- Title: 首页播放块增加点赞按钮
- Goal: 在首页播放块删除按钮前增加点赞按钮，点击当前酷狗歌曲时复用现有点赞逻辑。
- Why: 用户要求播放块操作和其它页面一致；当前只有删除按钮，缺少对当前播放歌曲的快速点赞入口。
- Responsibility Boundary:
  - XML/layout: 在删除按钮前增加 ImageButton。
  - UI接线: 根据当前 source/track 启用/禁用点赞按钮。
  - 点赞执行: 复用 `requestLikeTrack` / `LikeStatusStore` / `KugouDirectContentClient.addSongToLikeList`。
  - `MainActivity`: 只允许少量当前播放 SourceTrack 获取和按钮接线；若逻辑扩大则拆 `HomePlaybackActionsBinder`。
- Dependencies:
  - 现有酷狗点赞逻辑已存在。
  - `T-S5-HOME-143` 可并行，但共享首页 layout 需注意冲突。
- Inputs:
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/res/values/strings.xml`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/ui/SourceRowRenderer.kt`
- Expected Outputs:
  - 播放块删除按钮前出现点赞按钮。
  - 当前酷狗曲目可点，缺 session 时走现有登录恢复，失败/成功更新点赞状态页。
  - 非酷狗/空播放状态禁用或按既有能力提示。
- Expected Files:
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（接线-only）或新 `HomePlaybackActionsBinder.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 不新增第二套点赞状态；所有状态仍进 `LikeStatusStore`。
- Comment Requirements:
  - 无特别要求。
- Done Criteria:
  - 按钮位于删除按钮前。
  - 当前酷狗歌曲点击点赞与列表行点赞表现一致。
  - 空播放/不可点赞来源不会崩溃。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
- Risks:
  - 当前播放源快照为空或 Emby track 时误调用酷狗点赞；需 source-aware guard。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-DISC-145
- Task ID: `T-S5-DISC-145`
- Module ID: `M-S5-DISCOVER-044`
- Status: Ready
- Title: 发现页一级/二级分类模型与 `.NET` 行为对齐
- Goal: 梳理 Android 发现页所需的一级 tab、二级 tab、Scene/playlist 数据来源和切换加载契约，确保 UI 重构前数据边界清楚。
- Why: `.NET` UI 没有独立 Scene 页面，但底层存在 SceneClient；Android 需要在发现页 UI 合并两类能力，不能把接口来源混成不可维护的状态。
- Responsibility Boundary:
  - `kugou/*`: direct tags/playlists/scene 请求和解析。
  - `ui/*`: 分类模型和加载契约，不猜协议。
  - `KugouMusic.NET/`: 只读核对。
- Dependencies:
  - `T-S5-KG-123` Done。
  - `T-S5-SCENE-129` Done。
- Inputs:
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/Views/DiscoverView.axaml`
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/ViewModels/DiscoverViewModel.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/SceneClient.cs`
  - Android `KugouContentBinder/KugouSceneBinder/KugouSceneContentClient`
- Expected Outputs:
  - 发现页一级分类枚举：场景、主题、语种、风格、心情、年代。
  - 二级分类数据结构，能标记有图/无图、所属一级、展开策略、请求参数。
  - 切换一级/二级自动加载歌单的状态契约。
  - 独立 Scene 入口迁移/隐藏策略。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouDiscoverBinder.kt` 或现有 binder 扩展
  - `app/src/main/java/com/skodamusic/app/model/MainModels.kt` 或新 discover model
  - `.ai/context/CURRENT_STATUS.md`（执行后回写真实发现）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 如果一级/二级数据能完全来自现有 playlist tags，则 Scene direct 只作为“场景”一级的数据来源；如果字段不足，标记 Pending，不猜。
- Comment Requirements:
  - 对 `.NET` 来源对应关系加短注释/常量名。
- Done Criteria:
  - 数据模型支撑紧凑 UI，无需横向滚动。
  - 切换加载路径清晰。
  - 编译通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - `.NET` Avalonia Discover 和 Scene raw API 信息结构不同；合并 UI 时需保持来源标识，避免后续播放队列 source 混乱。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-DISC-146
- Task ID: `T-S5-DISC-146`
- Module ID: `M-S5-DISCOVER-044`
- Status: Ready
- Title: 发现页紧凑 tab + 歌单网格 UI 重构
- Goal: 将发现页改为小屏横屏友好的一级 tab、二级 tab 和歌单网格，去掉无用标题/说明/刷新按钮，并隐藏独立 Scene 用户入口。
- Why: 用户给出的 `.NET`/手机版酷狗参考是发现页两级 tab + 歌单列表；车机屏幕更小，应减少无用占用并避免横向滚动。
- Responsibility Boundary:
  - Renderer: 一级/二级 tab 多行布局、展开收缩、歌单网格卡片、缩略图占位。
  - Binder: 选中状态、切换自动加载、失败提示/点击重试、点击后二级收缩。
  - `MainActivity`: 左侧发现入口和 Scene 入口隐藏/移除接线。
- Dependencies:
  - `T-S5-DISC-145`
  - `RemoteThumbnailLoader` exists。
- Inputs:
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentRenderer.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouSceneBinder.kt`
- Expected Outputs:
  - 无“发现歌单”标题行、无“二级分类：xxx”行、无刷新按钮。
  - 一级 tab 全量显示。
  - 二级 tab 不横向滚动，有图两行/无图三行默认，多余展开/收缩，点击后收缩。
  - 切换一/二级自动获取歌单，下方网格展示缩略图歌单。
  - 独立 Scene 左侧入口不再用户可见。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouDiscoverRenderer.kt`（推荐拆分）
  - `app/src/main/java/com/skodamusic/app/ui/KugouDiscoverBinder.kt`（如需要）
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentRenderer.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/res/values/strings.xml`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/res/layout/activity_main.xml`（已有 warning，尽量复用容器+程序化渲染）
- Architecture Notes:
  - 网格列数应适配 1024x600，文本必须截断/换行，不可重叠。
  - 不引入第三方图片库。
- Comment Requirements:
  - 对 row-limit 策略常量命名即可，必要时短注释。
- Done Criteria:
  - 1024x600 横屏发现页首屏紧凑，能看到分类和至少部分歌单网格。
  - 分类无横向滚动，展开收缩可用。
  - 切换分类自动加载歌单。
  - 独立 Scene 入口不可见。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
  - 截图/设备 smoke：发现页无重叠、无多余标题/刷新。
- Risks:
  - 修改 `activity_main.xml` 可能触发布局回归；优先小步替换发现页容器内部。
- Size: L
- Execution Mode: Module
- Minimal Loop: No

## T-S5-OBS-147
- Task ID: `T-S5-OBS-147`
- Module ID: `M-S5-DISCOVER-044`
- Status: Ready
- Title: 首页/发现页纠偏观测与 API17 回归清单更新
- Goal: 为本轮首页每日推荐、队列跟随、歌词、点赞和发现页重构补齐脱敏观测与回归清单。
- Why: 用户已要求新增功能必须有可查日志；本轮多处行为要通过实机验证闭环。
- Responsibility Boundary:
  - 代码侧只记录低频 start/success/failure/state-change。
  - 文档侧更新事件字典、观测覆盖和 API17 交互清单。
- Dependencies:
  - `T-S5-HOME-141`
  - `T-S5-HOME-142`
  - `T-S5-HOME-143`
  - `T-S5-HOME-144`
  - `T-S5-DISC-146`
- Inputs:
  - `docs/S5_OBSERVABILITY_COVERAGE.md`
  - `docs/POSTHOG_EVENT_DICTIONARY.md`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
- Expected Outputs:
  - 覆盖 daily startup autoplay/list-only、queue current changed/scroll requested、lyrics search/download failed、lyrics idle switch、discover tab select/load、home like click。
  - 敏感字段审计：不记录 token、session、完整 hash、完整 URL/query、歌词原文、手机号。
  - API17/1024x600 回归条目。
- Expected Files:
  - docs files and necessary event call sites.
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 歌词原文不得进入 PostHog；只记录 stage/error/count/short ids。
- Comment Requirements:
  - 无特别要求。
- Done Criteria:
  - 文档与代码事件一致。
  - 本轮新增失败路径有 runtime/PostHog 证据。
  - `git diff --check` 通过。
- Validation:
  - `git diff --check`
  - 敏感字段 grep/review
  - `gradle :app:compileDebugKotlin --no-daemon`（若改代码）
- Risks:
  - 观测过多；避免 UI redraw、progress tick 和逐帧 DSP 状态上报。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-DSP-148
- Task ID: `T-S5-DSP-148`
- Module ID: `M-S5-DSP-045`
- Status: Ready
- Title: DSP 红圈原因显示与 runtime/logcat 采证补齐
- Goal: 在 UI/runtime log 中显示播放外圈红色对应的具体 DSP 原因，并补齐可用于定位“音效无效”的低频诊断。
- Why: 用户仍看到外圈持续红色且听感无音效；当前颜色只说明 FAIL_OPEN，缺少可见原因。
- Responsibility Boundary:
  - `HiFiDspController`/`HiFiAudioProcessor`: 输出稳定 reason/flags/last status。
  - `EqualizerPageBinder`/播放按钮状态显示: 展示简短状态和原因。
  - `MainActivity`: 只低频刷新，不解释 native flags。
- Dependencies:
  - `T-S4-AUDIO-097`
- Inputs:
  - `app/src/main/java/com/skodamusic/app/audio/dsp/HiFiAudioProcessor.kt`
  - `app/src/main/java/com/skodamusic/app/audio/dsp/HiFiDspController.kt`
  - `app/src/main/java/com/skodamusic/app/ui/EqualizerPageBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Expected Outputs:
  - 红/黄/灰状态有 short reason，例如 native-not-ready、native-process-error、unsupported-format、bypass、disabled、unknown。
  - runtime log 节流记录 status/reason/mode/tier/flags，不记录高频 frame。
  - API17 回归清单加入 `hifi-dsp` log 采样要求。
- Expected Files:
  - DSP controller/processor files
  - UI binder/main接线小改
  - docs/checklist
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 原因码归属 DSP controller，UI 只显示字符串/枚举映射。
  - 不按 audio frame 推 UI。
- Comment Requirements:
  - 对状态节流和 fail-open reason 保留短注释。
- Done Criteria:
  - 红圈时能看到或记录明确原因。
  - 关闭/原声/未知不误显示红色。
  - 构建通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
- Risks:
  - 无实机音频路径时只能验证诊断显示，不能证明音效已生效。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-DSP-149
- Task ID: `T-S5-DSP-149`
- Module ID: `M-S5-DSP-045`
- Status: Pending Confirmation / Evidence
- Title: DSP 音效无效 targeted fix
- Goal: 根据 `T-S5-DSP-148` 和实机 `hifi-dsp` 日志定位具体原因并修复音效无效/持续红圈。
- Why: 没有 reason/log 前无法判断是 native 未加载、format 不支持、direct buffer、process error、状态未复位还是真实 bypass。
- Responsibility Boundary:
  - 只修有证据的具体 DSP failure path。
  - 不做无证据的算法重写或高 API 音频栈替换。
- Dependencies:
  - `T-S5-DSP-148`
  - 实机或手机 logcat/runtime 样本。
- Inputs:
  - `hifi-dsp config/format/native active/native status/bypass/error` 日志。
  - UI 红圈 reason。
- Expected Outputs:
  - focused DSP fix。
  - 对应回归证据。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/audio/dsp/*`
  - `app/src/main/cpp/*`（如 native 证据指向 C++）
  - docs/checklist/status
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 热路径修改必须保持 fail-open 和 API17。
- Comment Requirements:
  - 对 native/fail-open 修复点保留必要注释。
- Done Criteria:
  - 红圈原因消失或变为正确状态颜色。
  - 正常 active 绿色，降级黄色，关闭/未知灰色，真实错误红色。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:assembleDebug --no-daemon`
  - 设备长播/听感/logcat 验证。
- Risks:
  - 若缺设备日志，本任务必须保持 Pending，不放 Ready。
- Size: M/L
- Execution Mode: Single
- Minimal Loop: No

## T-S5-VAL-137
- Task ID: `T-S5-VAL-137`
- Module ID: `M-S5-VAL-041`
- Status: Ready
- Title: S5 集成设备验证执行包与证据回填
- Goal: 在手机/API17 设备上验证当前 S5 本地完成链路，并将结果回填为可执行的失败分流。
- Why: 当前 direct 登录/content/home/scene/vip 均只完成本地编译和静态验证，仍缺真实账号、真实接口字段、车机 UI/性能和无权限歌曲分类证据。
- Responsibility Boundary:
  - 本任务只执行验证、采集证据、整理结论。
  - 不在证据不足时修改协议字段或 UI 逻辑。
  - 失败修复进入 `T-S5-TRIAGE-140` 后拆 targeted fix。
- Dependencies:
  - `M-S5-KG-037` Done locally。
  - `T-S5-KG-123` Done locally。
  - `M-S5-HOME-038` Done locally。
  - `M-S5-SCENE-039` Done locally。
  - `M-S5-VIP-040` Done locally。
- Inputs:
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
  - 当前 debug APK。
  - 手机/API17 设备、可用酷狗账号、可用网络。
- Expected Outputs:
  - 每组 PASS/FAIL 结果。
  - Scene/Radio/Discover/VIP 真实响应字段缺口。
  - PostHog/logcat/runtime 脱敏证据。
  - 下一轮 `T-S5-TRIAGE-140` 输入。
- Expected Files:
  - `.ai/context/CURRENT_STATUS.md`
  - `.ai/context/HANDOFF.md`
  - `.ai/context/NEXT_STEPS.md`
  - 可选 `docs/S5_DEVICE_VALIDATION_REPORT.md`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 设备失败要按 owner 分流：auth/content/scene/vip/playback/ui/observability。
  - PostHog 无 query token 时，用 logcat `SkodaPostHog` capture ok 和 runtime logs 作为替代证据。
- Comment Requirements:
  - 无代码改动，无新增注释要求。
- Done Criteria:
  - 至少覆盖 QR 登录、默认加载、Radio/Discover、Scene、每日推荐、当前队列、VIP、无权限播放提示。
  - 每个失败项包含复现步骤和至少一项证据。
  - context 回写可指导下一轮 execution。
- Validation:
  - 设备执行 `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md` K/M/G/N/P 关键条目。
  - 记录 logcat 命令与结果。
- Risks:
  - 设备窗口不可用会阻塞完成。
  - 无 VIP/无权限样本歌曲时 P4 只能部分验证。
- Size: M
- Execution Mode: Module
- Minimal Loop: No

## T-S5-MAIN-138
- Task ID: `T-S5-MAIN-138`
- Module ID: `M-S5-MAIN-042`
- Status: Done
- Title: `RuntimeLogBinder` 提取（MainActivity 红线治理首刀）
- Goal: 将 runtime log buffer、preview、fullscreen dialog、copy/clear 行为从 `MainActivity` 迁出到 focused binder。
- Why: `MainActivity.kt` 当前约 6009 行且仍有 code health blocking；RuntimeLog 是低耦合诊断 UI，适合作为 Phase 3 首刀，并为未来 Fragment 试点准备边界。
- Responsibility Boundary:
  - `RuntimeLogBinder`: log buffer、线程安全追加、preview 渲染、fullscreen dialog、copy/clear、销毁。
  - `MainActivity`: 初始化 binder、传入 showToast/getString/clipboard 所需 Activity context、把 `appendRuntimeLog` 委托给 binder。
  - 不迁移播放、下载、service bridge 或 PostHog 逻辑。
- Dependencies:
  - `T-S5-MAIN-116` Done，已推荐 RuntimeLog 作为首个 Binder 化对象。
- Inputs:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt` line 5517 附近 runtime log 方法。
  - `runtime_log_preview` 和 `runtime_log_label` 现有 XML ID。
- Expected Outputs:
  - 新增 `app/src/main/java/com/skodamusic/app/ui/RuntimeLogBinder.kt`。
  - `MainActivity` runtime log 状态和方法明显减少。
  - Runtime log 点击展开、复制、清空、预览保持现状。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/RuntimeLogBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（只允许删减和接线）
  - `app/src/main/res/layout/activity_main.xml`
- Architecture Notes:
  - Binder 内部持有 Activity/View 需要提供 `destroy()`，Activity `onDestroy` 调用以 dismiss dialog。
  - `appendRuntimeLog` 可能从后台线程调用，Binder 必须保留主线程 UI 更新防护。
- Comment Requirements:
  - 对后台线程追加日志和 UI 主线程同步策略保留简短注释。
- Done Criteria:
  - Runtime log preview 正常更新。
  - 点击 label/preview 可打开 dialog。
  - 复制/清空可用。
  - Activity destroy 时 dialog 被关闭。
  - `MainActivity.kt` 行数下降，且 code health blocking 不增加。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
  - `python scripts/check_code_health.py`（允许既有 blocking，但不得新增）
- Result:
  - 新增 `RuntimeLogBinder.kt`，`MainActivity` 只保留 append 委托。
  - 本地验证通过；`check_code_health.py` 仅保留既有 `MainActivity` red-line。
- Risks:
  - 多线程日志追加若处理不当会丢失 UI 更新或触发非主线程异常。
  - Dialog 生命周期若遗漏 dismiss 可能泄漏 Activity。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-MAIN-139
- Task ID: `T-S5-MAIN-139`
- Module ID: `M-S5-MAIN-042`
- Status: Done
- Title: `EqualizerPageBinder` 提取（音效页 Binder 化）
- Goal: 将 EQ fullscreen page controls、mode/preset/band rendering 和 back action 从 `MainActivity` 迁出。
- Why: EQ 页面是低于播放主链风险的页面壳候选；先 Binder 化可以继续压降入口文件并为 Fragment 试点准备边界。
- Responsibility Boundary:
  - `EqualizerPageBinder`: EQ 页面 View 绑定、状态渲染、模式按钮、返回回调。
  - `MainActivity`: 提供 `EqualizerManager`/`HiFiDspController` 状态读取和命令回调，不承载 UI 构造细节。
  - 不迁移 native DSP processor 或 playback engine。
- Dependencies:
  - `T-S5-MAIN-138` 完成。
- Inputs:
  - `MainActivity.kt` line 5375 起 EQ render/refresh methods。
  - `activity_main.xml` existing EQ IDs。
- Expected Outputs:
  - 新增 `app/src/main/java/com/skodamusic/app/ui/EqualizerPageBinder.kt`。
  - `MainActivity` 删除 EQ 页面 UI 构造细节。
- Result:
  - 新增 `EqualizerPageBinder.kt`，承接音效页开关、入口、返回、模式按钮和 fullscreen rendering。
  - `MainActivity` 保留 DSP apply/persist、feedback/toast 和页面切换委托。
  - 本地验证通过；`MainActivity.kt` 最新约 5755 行，code health blocking 未增加。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/EqualizerPageBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/audio/dsp/*`
- Architecture Notes:
  - 保持 API17-safe View API，不引入 Fragment 或 Navigation。
- Comment Requirements:
  - 对 DSP 状态到 UI 映射保留简短说明。
- Done Criteria:
  - EQ 页面打开/返回/切换模式不回归。
  - `MainActivity.kt` 行数继续下降。
  - 构建与 guardrails 通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
  - `python scripts/check_code_health.py`
- Risks:
  - EQ 页面和 DSP runtime 状态耦合，需避免把 DSP 控制逻辑搬进 UI binder。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-TRIAGE-140
- Task ID: `T-S5-TRIAGE-140`
- Module ID: `M-S5-VAL-041`
- Status: Planned
- Title: 真实设备失败分流与 targeted fix 计划
- Goal: 根据 `T-S5-VAL-137` 的实机证据，把失败项拆成具体、可执行、可验证的修复任务。
- Why: S5 目前协议面广，不能在没有真实失败证据时猜测修复；分流能避免把多个问题混在一个大补丁。
- Responsibility Boundary:
  - 本任务只做 triage 和计划更新。
  - 具体修复进入后续 focused task，例如 Scene 字段修复、VIP 响应解析修复、缩略图弱网修复等。
- Dependencies:
  - `T-S5-VAL-137`
- Inputs:
  - Device Report
  - logcat/runtime/PostHog 证据
  - 截图/视频说明
- Expected Outputs:
  - 更新 `.ai/context/TASK_QUEUE.md` Ready/Pending。
  - 更新 `.ai/context/CURRENT_STATUS.md` 和 `HANDOFF.md`。
- Expected Files:
  - `.ai/context/CURRENT_STATUS.md`
  - `.ai/context/TASK_QUEUE.md`
  - `.ai/context/NEXT_STEPS.md`
  - `.ai/context/HANDOFF.md`
- Files Not To Expand:
  - app source files
- Architecture Notes:
  - 修复任务必须归属对应 owner：`kugou/*`、`ui/*Binder`、`playback/*`、observability docs。
- Comment Requirements:
  - 无代码改动。
- Done Criteria:
  - 每个失败项有明确 owner、复现、证据、修复建议、优先级。
  - Ready 只放依赖满足且边界清晰的问题。
- Validation:
  - 文档一致性检查。
- Risks:
  - 证据不足时只能标 Pending Confirmation，不得放 Ready。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-HOME-128
- Task ID: `T-S5-HOME-128`
- Module ID: `M-S5-HOME-038`
- Status: Done 2026-06-07
- Title: 左侧每日推荐入口、酷狗模式隐藏 Emby 队列按钮、首次自动播放每日推荐
- Goal: 在左侧一级导航新增每日推荐入口；酷狗默认模式隐藏旧 Emby 队列按钮；首次进入首页自动加载每日推荐并播放第一首。
- Why: 用户明确要求新增的是左侧按钮，不是改首页中间播放块；每日推荐应成为酷狗默认首页体验，且不再暴露 Emby 队列语义干扰酷狗模式。
- Responsibility Boundary:
  - `MainActivity` 只负责按钮查找、导航委托和最小接线。
  - 每日推荐加载/首次播放 guard 放在 `KugouContentBinder` 小范围扩展或 `DailyRecommendCoordinator`。
  - 播放第一首必须走现有 Kugou queue/source session，不复用 Emby queue。
- Dependencies:
  - `T-S5-KG-122` Done。
  - `T-S5-PLAY-110/111` Done。
  - `M-S5-KG-037` Done locally。
- Inputs:
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentRenderer.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectContentClient.kt`
- Expected Outputs:
  - 左侧新增每日推荐按钮。
  - 酷狗模式下旧 `nav_queue`/Emby 队列入口隐藏或不再作为酷狗默认导航暴露。
  - 首次进入首页自动拉 `/everyday_song_recommend` 并播放第一首。
  - 移除每日推荐刷新按钮/刷新交互；当天一批数据复用 existing loading/cache guard。
  - 脱敏事件或 runtime log 覆盖 daily recommend auto-load/play-first start/success/failure。
- Expected Files:
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/res/values/strings.xml`
  - `app/src/main/java/com/skodamusic/app/ui/DailyRecommendCoordinator.kt`（如需要）
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（接线-only）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 不把“首次播放”状态散落在 Activity；用 coordinator 或 binder 内部单一 guard。
  - 若用户未登录，继续使用现有弹窗登录恢复；登录后恢复 daily recommend auto-play。
- Comment Requirements:
  - 对首次自动播放 guard 简短说明，避免重复自动起播。
- Done Criteria:
  - 冷启动/首次首页进入时每日推荐自动加载并播放第一首。
  - 再次进入不会重复抢播当前播放。
  - 左侧每日推荐入口可手动返回该列表。
  - 酷狗模式下旧 Emby 队列按钮不可见。
  - 无每日推荐刷新按钮。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
  - `python scripts/check_code_health.py`（允许既有 MainActivity red-line，但不得新增 red finding）
- Risks:
  - 自动播放可能与 QR 登录成功自动加载重复触发；需要去重。
  - 如果首次自动播放发生在未登录状态，需要通过 pending action 等待登录成功。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-SCENE-129
- Task ID: `T-S5-SCENE-129`
- Module ID: `M-S5-SCENE-039`
- Status: Done 2026-06-07
- Title: Scene `.NET` direct 来源映射与 Android client/模型边界
- Goal: 按 `KugouMusic.NET` `SceneClient` / `RawMediaCatalogApi` 确认 Scene direct API、请求参数和 Android 最小数据模型。
- Why: 用户要求“场景来源是什么你核对 `.NET` 怎么取”；Scene 不能在未核对协议和字段前直接 UI 实现。
- Responsibility Boundary:
  - `kugou/*`: direct 请求、签名参数、JSON 字段解析。
  - `ui/*`: 不在本任务实现完整网格 UI，只保留必要最小验证入口或接口供后续 UI 使用。
  - `KugouMusic.NET/`: 只读参考，不修改。
- Dependencies:
  - `KugouMusic.NET/` reference exists.
  - `KugouDirectContentClient` 已有 direct request patterns。
- Inputs:
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/SceneClient.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/RawMediaCatalogApi.cs`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectContentClient.kt`
  - `app/src/main/java/com/skodamusic/app/model/MainModels.kt`
- Expected Outputs:
  - Scene list/audio/module/music API 的 Android direct request implementation 或 focused client scaffold。
  - 最小 Scene model：scene id/title/subtitle/thumbnail/module/tag/audio source track mapping。
  - 失败路径返回明确 null/result，不抛到 UI。
  - 脱敏事件：scene list/audio request success/failure，不记录 token/session/full query。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/kugou/KugouSceneContentClient.kt` 或 `KugouDirectContentClient.kt` 小范围扩展
  - `app/src/main/java/com/skodamusic/app/model/MainModels.kt` 或新 scene model 文件
  - `docs/POSTHOG_EVENT_DICTIONARY.md`（若新增事件名）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 如果真实响应字段与 `.NET` JsonElement 未建强类型模型不一致，本任务应记录字段缺口，不猜测。
  - Scene API 如果需要 session token/userid，缺 session 时交给现有登录恢复策略。
- Comment Requirements:
  - 对可追溯到 `.NET` 的 endpoint/参数保留简短注释或常量命名。
- Done Criteria:
  - Android 端存在可调用的 Scene list + audio/module 最小 direct client。
  - 字段映射足够支撑后续 tab/grid UI。
  - 编译通过。
  - 日志/PostHog 脱敏。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 如能本地真实账号请求，记录脱敏 runtime log；否则标记待设备验证。
- Risks:
  - Scene API 可能需要真实 token 或额外签名字段；若 `.NET` 依据不足，停下标记 Blocked。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-HOME-130
- Task ID: `T-S5-HOME-130`
- Module ID: `M-S5-HOME-038`
- Status: Done 2026-06-07
- Title: 首页右侧当前队列面板与所有来源自动滚动到当前歌曲
- Goal: 将首页右侧从推荐列表改为当前播放列表/队列，并对每日推荐、普通歌单、Radio、Scene、Emby 显式来源自动滚动到当前歌曲。
- Why: 用户要求首页右侧作为当前播放列表，不是推荐列表；所有类型播放列表都应自动滚动到当前歌曲。
- Responsibility Boundary:
  - 新增 `HomeQueuePanelBinder` 或等价类负责右侧队列 UI 和自动滚动。
  - 队列数据来自现有 source session/queue managers，不在 UI binder 里重新计算播放语义。
  - `MainActivity` 只通知播放状态变化或绑定 callbacks。
- Dependencies:
  - `T-S5-HOME-128`
  - `T-S5-SCENE-129`（Scene 来源队列需要）
- Inputs:
  - `KugouPlaybackQueueManager.kt`
  - `KugouRadioSessionManager.kt`
  - `SourcePlaybackSession.kt`
  - `activity_main.xml` 首页右侧 panel
- Expected Outputs:
  - `HomeQueuePanelBinder`。
  - source-aware queue display adapter。
  - Radio current/upcoming/history 展示。
  - `post { scrollTo/currentRow }` 或 API17-safe 等价自动滚动。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/HomeQueuePanelBinder.kt`
  - `app/src/main/res/layout/activity_main.xml`
  - `app/src/main/res/values/strings.xml`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 自动滚动只在当前歌曲变化或列表重渲染后触发，避免高频 UI 滚动。
- Comment Requirements:
  - 对延迟滚动的 API17/layout timing guard 保留简短说明。
- Done Criteria:
  - 首页右侧显示当前队列而非推荐列表。
  - 普通队列、Radio、每日推荐、Scene、Emby 显式队列均能定位当前歌曲。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
- Risks:
  - 当前队列状态来源分散；需要小 adapter，不要把所有来源分支塞进 Activity。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-UI-131
- Task ID: `T-S5-UI-131`
- Module ID: `M-S5-SCENE-039`
- Status: Done 2026-06-07
- Title: Radio/Scene 缩略图网格与 Scene tab 展开收缩
- Goal: 将 Radio 和 Scene 内容入口改为带缩略图的网格卡片，并实现 Scene tab 非横向滚动、展开/收缩、点击后收缩。
- Why: 用户反馈电台和场景未显示为网状、缺缩略图；Scene 分类不能横向滚动。
- Responsibility Boundary:
  - Renderer 负责网格和 tab UI。
  - Binder 负责 selected/expanded/loading state。
  - Client 负责 thumbnail/title 数据。
- Dependencies:
  - `T-S5-SCENE-129`
  - `T-S5-HOME-128`
- Inputs:
  - `KugouContentRenderer.kt`
  - `KugouContentBinder.kt`
  - `SourceRadio` / Scene models
  - `activity_main.xml`
- Expected Outputs:
  - Radio grid card rendering with thumbnails.
  - Scene grid card rendering with thumbnails.
  - Scene tabs show two or three rows by default; expand/collapse; click collapses.
  - 1024x600 layout stable with no text overlap.
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentRenderer.kt` 或新 grid renderer
  - `app/src/main/java/com/skodamusic/app/ui/KugouSceneBinder.kt` 或 binder 扩展
  - `app/src/main/res/layout/activity_main.xml`
  - 必要 drawable/string
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`（若大幅增加则拆新 binder）
- Architecture Notes:
  - 不引入第三方图片库；若需要图片下载，使用已有 OkHttp/Bitmap 路径并做内存/失败保护。
- Comment Requirements:
  - 对 tab row limit/expand policy 可用常量命名，少量注释即可。
- Done Criteria:
  - Radio/Scene 显示网格缩略图。
  - Scene tab 不横向滚动，展开收缩正常，点击后收缩。
  - API17 build pass。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
- Risks:
  - XML 文件长度已有 warning；如布局复杂，应优先 programmatic renderer 或拆轻量自定义 builder。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-OBS-132
- Task ID: `T-S5-OBS-132`
- Module ID: `M-S5-SCENE-039`
- Status: Done 2026-06-07
- Title: 首页/Scene/当前队列交互观测与 API17 回归清单
- Goal: 为每日推荐自动播放、Scene 加载、grid/tab 交互、队列自动滚动补齐脱敏观测和回归清单。
- Why: 用户已明确后续新增功能必须有足够 PostHog/runtime/logcat 诊断日志且过滤敏感信息。
- Responsibility Boundary:
  - 代码侧只记录低频 start/success/failure/state-change。
  - 文档侧同步事件字典、覆盖矩阵、API17 测试项。
- Dependencies:
  - `T-S5-HOME-128`
  - `T-S5-SCENE-129`
  - `T-S5-HOME-130`
  - `T-S5-UI-131`
- Inputs:
  - `docs/POSTHOG_EVENT_DICTIONARY.md`
  - `docs/S5_OBSERVABILITY_COVERAGE.md`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
- Expected Outputs:
  - 事件/日志覆盖：daily recommend auto play、scene list/audio load、scene tab expand/collapse/select、queue auto-scroll。
  - 敏感字段审计记录。
  - API17 回归清单覆盖 1024x600 网格、tab、自动滚动。
- Expected Files:
  - docs and necessary observability call sites.
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 不为每次滚动像素或 UI redraw 上报 PostHog；只记录 current item changed/scroll requested 等低频事件。
- Comment Requirements:
  - 无特别要求。
- Done Criteria:
  - 文档与代码事件一致。
  - 敏感字段过滤通过。
  - `git diff --check` 通过。
- Validation:
  - `git diff --check`
  - 敏感字段 grep/review
  - `gradle :app:compileDebugKotlin --no-daemon`（若改代码）
- Risks:
  - 事件过多；优先复用 `kugou_content_load_*` stage。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-KG-124
- Task ID: `T-S5-KG-124`
- Module ID: `M-S5-KG-037`
- Status: Done (local validation passed; device validation pending)
- Title: 登录弹窗与 post-login 默认页自动加载协调
- Goal: 将首页登录入口改为弹窗登录，并在 QR 登录成功后自动触发首页推荐歌曲加载。
- Why: 当前车机登录成功后内容不会自动出现；用户期望默认页优先自动加载。
- Responsibility Boundary:
  - `KugouAuthConfigBinder` 负责登录弹窗/QR UI 与登录成功通知。
  - `KugouContentBinder` 负责默认页推荐歌曲加载。
  - 如需跨 binder 状态，新增 `KugouLoginRecoveryCoordinator` 或等价小类；`MainActivity` 仅接线。
- Dependencies:
  - `T-S5-KG-122` Done。
  - `KugouAuthConfigBinder`、`KugouContentBinder` 已存在。
- Inputs:
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/res/layout/activity_main.xml`
- Expected Outputs:
  - 首页登录入口触发弹窗登录。
  - QR 登录成功后触发 `requestRecommendedSongs()`。
  - 登录成功后弹窗隐藏，首页推荐加载状态可见。
  - 新增脱敏 runtime/PostHog 事件：post-login auto-load start/success/failure 或复用内容加载事件并补 stage。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouLoginRecoveryCoordinator.kt`（如需要）
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（接线-only）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 弹窗 lifecycle 必须和 Activity 生命周期绑定，stop/destroy 时关闭或失效化回调。
  - 不把 post-login action 状态机写入 `MainActivity`。
- Comment Requirements:
  - 若新增 coordinator，说明它只负责登录恢复动作排队，不持有长期业务数据。
- Done Criteria:
  - 首页未登录时点击登录触发弹窗。
  - 扫码成功后自动拉首页推荐歌曲。
  - 失败时提示但登录态不被清除。
  - `git diff --check`、API17 guardrails、`compileDebugKotlin` 通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 触及 layout/dialog 时执行 `gradle :app:assembleDebug --no-daemon`
- Risks:
  - Dialog 在 API17/Activity 生命周期中泄漏。
  - 自动加载重复触发导致并发请求；需利用 existing loading guard 或 generation。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-KG-125
- Task ID: `T-S5-KG-125`
- Module ID: `M-S5-KG-037`
- Status: Done (local validation passed; direct API gaps deferred to `T-S5-KG-123`)
- Title: 酷狗内容页懒加载与失败可重试策略
- Goal: 推荐电台、发现、点赞/相关页面进入时自动懒加载；失败提示并保留手动重试入口。
- Why: 用户要求其它页进入则加载，而不是登录后全量请求或永远空白。
- Responsibility Boundary:
  - `KugouContentBinder` 拥有内容页 loaded/loading/failed 状态与 retry 入口。
  - 页面导航只通知当前页进入；`MainActivity` 不做懒加载判断。
  - 旧 WebApi baseUrl gate 不在本任务猜 direct API；若数据仍因 direct 缺口不可用，标记依赖 `T-S5-KG-123`。
- Dependencies:
  - `T-S5-KG-124`
- Inputs:
  - `KugouContentBinder.kt`
  - `KugouContentRenderer.kt`
  - 左侧导航进入 radio/discover/like 的现有委托
- Expected Outputs:
  - `onPageEntered(page)` 或等价懒加载入口。
  - 推荐电台/发现页进入时按缺失状态自动加载。
  - 失败状态不清空旧内容，提供可点击或现有按钮式重试入口。
  - 旧 direct 缺口用明确反馈，不提示用户填写 baseUrl。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentRenderer.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`（导航接线-only）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 懒加载状态应以页面为粒度，避免一个全局 loading 阻塞所有页。
  - Radio/Discover direct API 缺口保留给 `T-S5-KG-123`。
- Comment Requirements:
  - 对“失败不清旧内容”的状态处理保留简短说明。
- Done Criteria:
  - 进入推荐电台页会触发推荐电台加载或显示 direct 缺口提示。
  - 进入发现页会触发 tags/playlists 加载或显示 direct 缺口提示。
  - 加载失败不清空已有列表。
  - 可手动重试。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - 旧 WebApi paths 可能仍不可用；本任务要避免把不可用误报成登录失效。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-KG-126
- Task ID: `T-S5-KG-126`
- Module ID: `M-S5-KG-037`
- Status: Done (local validation passed; server auth-invalid classification pending direct API work)
- Title: token/session 失效弹窗恢复，不清内容
- Goal: 运行中 session 失效时保留现有内容，弹窗登录；登录成功后恢复当前页面/当前列表加载或重试。
- Why: 当前部分内容请求失败会 `clearSessionState(true)` 并清空内容，和用户确认口径冲突。
- Responsibility Boundary:
  - `KugouContentBinder` 识别需要登录的失败，并请求登录恢复，不直接清内容。
  - `KugouAuthConfigBinder` 负责弹窗登录和成功回调。
  - `KugouLoginRecoveryCoordinator` 或等价类记录 pending page/action。
  - 显式登出仍可清内容；token 失效不可走同一路径。
- Dependencies:
  - `T-S5-KG-124`
  - `T-S5-KG-125`
- Inputs:
  - `KugouContentBinder.kt` 中 `clearSessionState(true)` / `requestQrLogin()` 的失败路径
  - `KugouAuthConfigBinder.kt`
  - `KugouDirectSessionStore.kt`
- Expected Outputs:
  - 区分 explicit logout 与 runtime auth invalid。
  - token/session 失效时不调用清内容路径。
  - 登录成功后恢复 pending 页面/列表加载。
  - 失败/恢复事件脱敏记录。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouLoginRecoveryCoordinator.kt` 或等价类
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 不把 token 本身或完整请求上下文保存到 pending action。
  - pending action 只保存页面/动作枚举和必要的非敏感短状态。
- Comment Requirements:
  - 对 pending action 不保存敏感信息的原因保留说明。
- Done Criteria:
  - 模拟内容请求 auth invalid 时旧内容仍显示。
  - 弹窗登录出现。
  - 登录成功后弹窗隐藏并重试当前页面/列表。
  - 显式登出仍清登录态和内容。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - `gradle :app:assembleDebug --no-daemon`
- Risks:
  - 需要准确区分网络失败、direct API 缺口、auth invalid；无法确认的错误不要清 session。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-OBS-127
- Task ID: `T-S5-OBS-127`
- Module ID: `M-S5-KG-037`
- Status: Done
- Title: 登录后加载/懒加载/token 恢复观测与回归清单
- Goal: 为新登录恢复流程补齐 PostHog/runtime/logcat 证据和 API17 回归项。
- Why: 用户已明确新功能必须有足够日志，且敏感信息过滤。
- Responsibility Boundary:
  - 代码侧仅记录低频事件。
  - 文档侧同步事件字典、覆盖矩阵、API17 检查项。
- Dependencies:
  - `T-S5-KG-124`
  - `T-S5-KG-125`
  - `T-S5-KG-126`
- Inputs:
  - `docs/POSTHOG_EVENT_DICTIONARY.md`
  - `docs/S5_OBSERVABILITY_COVERAGE.md`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
- Expected Outputs:
  - 新事件或复用事件 stage 记录：post_login_auto_load、lazy_load、auth_recovery_dialog、auth_recovery_resume。
  - 文档明确禁止 token/session/cookie/full query/phone/code/auth header。
  - 回归清单覆盖登录成功自动拉、进入页懒加载、失败重试、token 失效弹窗恢复。
- Expected Files:
  - `docs/POSTHOG_EVENT_DICTIONARY.md`
  - `docs/S5_OBSERVABILITY_COVERAGE.md`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
  - 必要代码埋点文件
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 不为高频 UI 渲染或每次导航 tick 上报。
- Comment Requirements:
  - 无特别要求。
- Done Criteria:
  - 事件字典、覆盖矩阵、回归清单与代码事件一致。
  - 敏感字段审计通过。
  - `git diff --check` 通过。
- Validation:
  - `git diff --check`
  - 敏感字段 grep/review
  - `gradle :app:compileDebugKotlin --no-daemon`（若改代码）
- Risks:
  - 事件过多；应复用现有 `kugou_content_load_*` 并增加 stage 时优先。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-MAIN-108
- Task ID: `T-S5-MAIN-108`
- Module ID: `M-S5-MAIN-029`
- Title: MainActivity 第一轮拆分边界落地
- Goal: 将 `MainActivity` 中低风险、与后续纠偏强相关的职责拆到独立类，减少继续修改 6443 行 Activity 的风险。
- Why: 用户已明确要求优化 `MainActivity` 过大问题；后续酷狗队列、电台和 service state 纠偏如果继续堆在 Activity 中会扩大回归面。
- Dependencies: 无
- Inputs:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/model/MainModels.kt`
  - `app/src/main/res/layout/activity_main.xml`
- Expected Outputs:
  - 新增一个或多个低风险 helper/binder/controller 类。
  - `MainActivity.kt` 行数明显下降。
  - 第一轮当时保持现有控件 ID、单 Activity 壳和用户可见流程；该约束不作为后续长期红线。
  - 为后续 `KugouQueueManager` / `KugouRadioSessionManager` 接入留出清晰边界。
- Done Criteria:
  - `git diff --check` 通过。
  - `./scripts/check_api17_guardrails.sh` 通过。
  - `gradle :app:compileDebugKotlin --no-daemon` 通过。
  - 左侧导航、播放按钮、设置页、酷狗登录入口仍可编译接线。
- Risks:
  - 拆分 UI binder 时容易捕获 Activity 生命周期；需要避免长生命周期持有已销毁 UI。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-MAIN-114
- Task ID: `T-S5-MAIN-114`
- Module ID: `M-S5-MAIN-034`
- Title: MainActivity 第二轮拆分：Kugou Auth/Config Binder
- Goal: 将 `MainActivity` 中酷狗登录、session 缓存、扫码/短信登录、状态文案和 WebApi 配置 UI 接线迁出到独立 binder/controller，给后续 `T-S5-KG-109` 留出清晰边界。
- Why: `MainActivity.kt` 第一轮后仍约 6213 行；下一步酷狗 API 地址纠偏会集中触碰登录/配置路径，如果继续直接改 Activity，会扩大 god Activity 风险。
- Responsibility Boundary:
  - 新类负责酷狗登录/配置相关 UI 绑定、状态刷新、扫码轮询生命周期入口和回调封装。
  - `MainActivity` 保留生命周期调用、页面切换协调和播放/service bridge，不承载登录 UI 细节。
  - 本任务不改变 `.NET` API 语义，不在本任务内猜测或重写酷狗协议。
- Dependencies: `T-S5-MAIN-108`
- Inputs:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouSessionStore.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouWebApiClient.kt`
  - `app/src/main/res/layout/activity_main.xml`
- Expected Outputs:
  - 新增 `KugouAuthConfigBinder.kt` 或等价小类。
  - `MainActivity` 中酷狗登录/配置相关字段和函数明显减少。
  - 扫码、短信、登出、session 恢复入口仍可编译接线。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - Binder 必须显式支持 `start/stop/destroy` 或等价生命周期清理，避免 QR polling 持有销毁后的 Activity/View。
  - 不引入 Fragment/多 Activity；页面壳拆分另由 `T-S5-MAIN-116` 评估。
- Comment Requirements:
  - 仅在轮询生命周期或回调边界处保留必要说明。
- Done Criteria:
  - `MainActivity.kt` 行数下降，不再继续承载酷狗登录/配置 UI 细节。
  - 默认酷狗登录入口、扫码刷新、短信登录、登出和状态显示编译接线不回归。
  - `git diff --check` 通过。
  - `./scripts/check_api17_guardrails.sh` 通过。
  - `gradle :app:compileDebugKotlin --no-daemon` 通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - QR polling 与 Handler 生命周期迁移可能引入泄漏或重复轮询；需保持 stop 行为。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes

## T-S5-MAIN-115
- Task ID: `T-S5-MAIN-115`
- Module ID: `M-S5-MAIN-034`
- Title: MainActivity 第三轮拆分：Kugou Content Pages Binder
- Goal: 将推荐歌曲、推荐电台、发现歌单/歌曲的页面状态、加载入口和渲染协调从 `MainActivity` 迁出。
- Why: 酷狗内容页仍占据 Activity 大量函数；后续普通队列和电台 session 都会改这些页面入口，必须先收敛边界。
- Responsibility Boundary:
  - 新 binder 负责酷狗内容页状态和渲染协调。
  - API client 仍保持独立；`.NET` 行为依据不在 binder 中硬编码猜测。
  - `MainActivity` 只接收“播放某 source track / 点赞 / 进入 radio session”等高层回调。
- Dependencies: `T-S5-MAIN-114`, `T-S5-KG-109`
- Inputs:
  - `MainActivity.kt` 中 `requestKugouRecommendedSongs`、`requestKugouRecommendedRadios`、`requestKugouRadioSongs`、`renderKugouRadioPage`、`requestKugouDiscoverTags`、`requestKugouPlaylistsByTag`、`requestKugouPlaylistSongs`、`renderKugouDiscoverPage`、`renderKugouRecommendedSongs`
  - `SourceRowRenderer.kt`
  - `KugouWebApiClient.kt`
- Expected Outputs:
  - 新增 `KugouPagesBinder.kt` 或等价类。
  - `MainActivity` 不再维护大批酷狗内容页 list/loading/selected state。
  - 推荐歌曲、电台、发现歌单仍走左侧一级页面，不增加二级 tab。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouPagesBinder.kt`
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 电台内容页不能直接落成普通歌曲队列；播放语义留给 `T-S5-PLAY-112`。
- Comment Requirements:
  - 对内容页状态所有权和回调边界保留简短说明。
- Done Criteria:
  - 推荐歌曲、推荐电台、发现页加载/渲染主逻辑迁出。
  - `MainActivity` 行数继续下降。
  - `git diff --check`、API17 guardrails、`compileDebugKotlin` 通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - 内容页状态与播放入口耦合，拆分时不能改变普通歌曲/电台后续分流语义。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 新增 `KugouContentRenderer.kt` 与 `KugouContentBinder.kt`。
  - 推荐歌曲、推荐电台、Radio songs、发现 tags/playlists/songs 的渲染、请求、loading/list/selected state 已迁出 `MainActivity`。
  - `MainActivity.kt` 约 6118 -> 5632 行，只保留内容页委托和播放/点赞高层回调。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`；`check_code_health.py` 仍因既有 `MainActivity` red-line 失败，blocking finding 数为 2。

## T-S5-MAIN-116
- Task ID: `T-S5-MAIN-116`
- Module ID: `M-S5-MAIN-034`
- Title: 页面壳拆分试点评估（Fragment / 独立 Activity）
- Goal: 基于已完成的 Controller/Binder 拆分，选择一个低耦合页面做 Fragment/独立 Activity 可行性评估或试点，明确后续是否继续页面化拆分。
- Why: 用户已明确单 Activity 外壳不是硬约束；但播放页、service bridge、方向盘按键和浮窗风险高，必须先用低耦合页面验证 API17 路径。
- Responsibility Boundary:
  - 评估候选：设置页、日志页、音效/EQ 页、酷狗内容页。
  - 不触碰播放热路径和 service bridge。
  - 不破坏左侧一级快速切换，不新增二级 tab。
- Dependencies: `T-S5-MAIN-114`, `T-S5-MAIN-115`
- Inputs:
  - `activity_main.xml`
  - `MainActivity.kt`
  - `app/build.gradle.kts`
  - AndroidX/AppCompat 现有依赖。
- Expected Outputs:
  - 页面壳拆分路线结论：Fragment、独立 Activity、继续 Binder 化，三者择一或给出暂缓原因。
  - 若安全，完成一个低耦合页面试点；若不安全，写明阻塞和后续条件。
- Expected Files:
  - 待任务执行时根据候选确定。
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - API17 兼容优先；不能为了页面化引入高 API 导航依赖。
- Comment Requirements:
  - 若引入 Fragment/Activity 试点，需要标注生命周期和回退路径。
- Done Criteria:
  - 有明确页面壳拆分决策和验证结果。
  - 若落地试点，构建通过并保持左侧一级切换体验。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 触及 manifest/resource 时执行 `gradle :app:assembleDebug --no-daemon`
- Risks:
  - Fragment/独立 Activity 会引入生命周期、状态同步和车机返回键行为风险；必须低耦合试点。
- Size: M
- Execution Mode: Module
- Minimal Loop: No
- Status: Done 2026-06-04
- Result:
  - 新增 `docs/PAGE_SHELL_SPLIT_EVALUATION.md`。
  - 结论：当前阶段暂缓独立 Activity；Fragment 可作为未来试点，但不直接迁移 raw XML 页面。
  - 推荐路线：先继续低耦合 Binder 提取，首选 `RuntimeLogBinder`，其次 `EqualizerPageBinder`；之后用已成型 Binder 包装 Fragment。
  - 理由：保持 API17、左侧一级导航、后台播放、方向盘按键、前台通知和浮窗状态稳定，避免在 S5 纠偏收尾阶段引入新生命周期风险。
  - 本地验证通过 `git diff --check` 与 API17 guardrails；`check_code_health.py` 仍因既有 `MainActivity` red-line 失败，blocking finding 数为 2。

## T-S5-KG-109
- Task ID: `T-S5-KG-109`
- Module ID: `M-S5-KG-030`
- Title: Kugou WebApi Base URL 产品路径纠偏与 direct API 可行性确认
- Goal: 移除用户必填 Kugou WebApi Base URL 的使用路径，并按 `KugouMusic.NET` direct client/raw api 确认 Android 可落地方案。
- Why: 用户质疑“酷狗 API 怎么要我提供地址了”；当前实现不符合“参考 .NET，不自行发挥”的要求。
- Dependencies: `T-S5-MAIN-114`
- Updated Dependencies: bootstrap red-line guardrails 生效后，本任务必须等待 `T-S5-MAIN-114` 完成；如果执行中仍需改登录/配置 UI，必须通过 `KugouAuthConfigBinder` 边界完成，不能直接扩大 `MainActivity`。
- Inputs:
  - `app/src/main/java/com/skodamusic/app/kugou/KugouWebApiClient.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouSessionStore.kt`
  - `activity_main.xml`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Infrastructure/Http/KgHttpTransport.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/`
- Expected Outputs:
  - 不再要求用户在设置页填写 WebApi Base URL。
  - 能迁移的默认 host/API 迁移到 Android 内部 adapter。
  - 无法确认的接口列出具体 `.NET` 缺口并标记 Blocked，不继续猜。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/kugou/KugouWebApiClient.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouSessionStore.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/res/values/strings.xml`
  - `app/src/main/res/layout/activity_main.xml`（仅允许移除/隐藏用户必填地址入口，不做布局大改）
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - API/config 纠偏归属 `kugou/*` 与 `KugouAuthConfigBinder`，入口文件只做委托。
  - 不修改 `KugouMusic.NET/`。
- Comment Requirements:
  - 若保留临时兼容路径，必须注释说明原因、`.NET` 依据和后续移除条件。
- Done Criteria:
  - 设置页无“必填 Kugou WebApi Base URL”阻断。
  - 酷狗登录/内容/播放 URL 路径要么有 direct `.NET` 依据，要么明确阻塞。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `python scripts/check_code_health.py`（允许既有 `MainActivity` red 继续存在，但本任务不得增加 red findings）
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - C# raw api 可能包含签名/加密/设备参数，Android 移植需要拆成后续任务。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 设置页已移除用户必填 WebApi Base URL。
  - Android 启动/登出清理旧 WebApi base-url/session 缓存。
  - QR/SMS 登录入口改为 direct API pending 状态，不再调用旧代理路由。
  - `.NET` direct API 需要签名、dfid/mid、cookie/session、AES/RSA 登录加密，已拆为 `T-S5-KG-117`。

## T-S5-KG-117
- Task ID: `T-S5-KG-117`
- Module ID: `M-S5-KG-030`
- Title: Kugou direct raw API 最小登录链路移植
- Goal: 基于 `KugouMusic.NET` direct/raw 实现 Android 最小登录链路，使 QR 登录或手机号验证码登录不依赖用户填写 WebApi 地址。
- Why: `T-S5-KG-109` 已移除 WebApi 地址产品路径，但酷狗真实登录/session 仍待 direct raw client 支撑。
- Dependencies: `T-S5-KG-109`
- Inputs:
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Infrastructure/Http/KgHttpTransport.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Infrastructure/Http/Handlers/SignatureHandler.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/RawLoginApi.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Transport/KgRequest.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Session/KgSessionManager.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/util/KGSigner.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/util/KGCrypto.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/util/KuGouConfig.cs`
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/`
- Expected Outputs:
  - Android-owned direct transport/request/signature/session scaffold.
  - 最小 QR 或 SMS 登录链路可从官方 raw endpoint 获取状态。
  - session 持久化格式明确，且不存储用户填写 WebApi Base URL。
  - 不能端到端确认的 raw 字段需列明阻塞点，不猜测。
- Expected Files:
  - 新增/替换 `app/src/main/java/com/skodamusic/app/kugou/*Direct*` 或等价 focused client。
  - `KugouAuthConfigBinder.kt` 只做 UI 状态和调用，不承载协议细节。
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - direct client 与 WebApi proxy client 必须区分清楚；不得重新暴露用户填写地址。
  - 登录 crypto/signature 逻辑必须可追溯到 `.NET` 源码。
- Done Criteria:
  - 至少一个登录入口具备 direct raw API 实现依据并可本地编译。
  - 若无法验证真实登录，需有明确 blocker 和下一步字段清单。
  - 旧 WebApi Base URL 路径不回归。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `python scripts/check_code_health.py`（允许既有 `MainActivity` red 继续存在，但不得新增 red findings）
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - AES/RSA 与签名细节移植可能较大，超出 single task 时应只落地 transport/signature scaffold 并拆分后续。
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 新增 Android direct QR client/signature/session store。
  - QR key/check 已按 `.NET` raw endpoint 与 Web signature 接入。
  - `KugouAuthConfigBinder` 可获取二维码、展示图片、2s 轮询，并缓存 QR success 返回的 direct token。
  - SMS AES/RSA、device init、token refresh/session validation 未在本轮实现，拆为 `T-S5-KG-118`。

## T-S5-KG-118
- Task ID: `T-S5-KG-118`
- Module ID: `M-S5-KG-030`
- Title: Kugou direct device init / token refresh / session validation
- Goal: 基于 `KugouMusic.NET` 补齐 QR success 后的 device init、token refresh 和 direct session 校验，使 direct 登录状态可支撑后续内容接口。
- Why: `.NET LoginViewModel` 在 QR success 后会执行 `deviceClient.InitDeviceAsync()` 与 `authClient.RefreshSessionAsync()`；Android 当前只缓存 QR check 返回 token。
- Dependencies: `T-S5-KG-117`
- Inputs:
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/ViewModels/LoginViewModel.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/LoginClient.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Clients/RegisterClient.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/RawDeviceApi.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/Protocol/Raw/RawLoginApi.cs`
  - `KugouMusic.NET/src/Libraries/KuGou.Net/util/KGCrypto.cs`
  - `app/src/main/java/com/skodamusic/app/kugou/`
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
- Expected Outputs:
  - direct session fields needed for authenticated content are defined and persisted.
  - QR success path performs or explicitly blocks device init/token refresh.
  - refresh failure clears direct session and returns to login state.
- Expected Files:
  - focused `kugou/*Direct*` additions; Binder only coordinates UI/state.
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Done Criteria:
  - direct session validation is no longer just cached QR token, or unresolved AES/RSA/device fields are explicitly blocked with `.NET` references.
  - old WebApi Base URL path does not return.
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `python scripts/check_code_health.py`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - `RawDeviceApi` and `RefreshTokenAsync` require AES/RSA helpers; if full port is too large, stop at a verified crypto scaffold and mark remaining fields blocked.
- Size: M
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 新增 `KugouDirectCrypto`，按 `.NET KgCrypto` 移植 AES-CBC、Playlist AES、RSA no-padding 和 RSA PKCS1（API17 标准库）。
  - 新增 `KugouDirectSessionClient`，按 `.NET RawDeviceApi.RegisterDevAsync` 与 `RawLoginApi.RefreshTokenAsync` 执行 device register + token refresh。
  - `KugouDirectSessionStore` 扩展持久化 `dfid/mid/uuid/installGuid/installMac/installDev/vipType/t1/validationState`。
  - `KugouAuthConfigBinder` 改为 QR success 后先进入 session validation；只有 token refresh 成功后 `hasSession()` 才为 true。
  - 未恢复用户填写 WebApi Base URL；未修改 `KugouMusic.NET/`；SMS 仍待 `LoginByMobileAsync` direct port。

## T-S5-KG-119
- Task ID: `T-S5-KG-119`
- Module ID: `M-S5-KG-035`
- Title: QR refresh crash hotfix + fail-soft observability
- Goal: 修复手机环境点击“刷新二维码”后约 1-2 秒崩溃的问题，并补齐 QR direct auth 关键路径的脱敏 PostHog/runtime/logcat 诊断。
- Why: QR 登录入口不稳定会阻塞 API17 实机 A~N 回归；当前崩溃发生在登录前置路径，必须先让刷新失败可恢复、不闪退。
- Responsibility Boundary:
  - `KugouAuthConfigBinder` 负责按钮状态、UI 状态、轮询生命周期、generation guard 和 Activity/View 生命周期保护。
  - `KugouDirectAuthClient` / `KugouDirectSessionClient` 负责 direct 网络、解析、crypto/session 请求，不向 UI 线程传播未捕获异常。
  - PostHog 只记录低频脱敏事件；runtime/logcat 只记录短 ID、阶段、错误码、异常类型和截断摘要。
  - 不修改 `MainActivity` 承载 QR 细节，不修改 `KugouMusic.NET/`。
- Dependencies: `T-S5-KG-118`
- Inputs:
  - 用户反馈：手机打开后点击刷新二维码，1-2 秒后崩溃；API17 实机未测。
  - 可选输入：`adb logcat` 中 `FATAL EXCEPTION` / `Caused by` 堆栈。
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectAuthClient.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectSessionClient.kt`
  - `app/src/main/java/com/skodamusic/app/observability/PostHogTracker.kt`
- Expected Outputs:
  - QR refresh 异步链路中网络、解析、bitmap download、UI 回调、polling start、session validation 的异常 fail-soft。
  - 防重复点击/并发刷新导致旧回调覆盖新状态。
  - 失败时 UI 进入“失败/可重试”状态，而不是崩溃。
  - PostHog 或 runtime/logcat 至少覆盖 QR refresh start/success/failure、polling failure、session validation failure。
  - 回归清单补充 QR refresh crash 与脱敏观测验收项。
- Expected Files:
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectAuthClient.kt`
  - `app/src/main/java/com/skodamusic/app/kugou/KugouDirectSessionClient.kt`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
  - `KugouMusic.NET/`
- Architecture Notes:
  - 如需复用 PostHog 属性构造，优先增加小型 helper 或私有方法，不把埋点散落成长函数。
  - PostHog 上报必须 fail-open；上报失败不得影响 QR 登录。
  - 敏感字段禁止进入事件属性和 runtime log：token/session key/cookie/手机号/验证码/完整 URL query/auth header/API key/可复用设备凭据。
- Comment Requirements:
  - 对 lifecycle generation guard、并发刷新取消/忽略旧结果的逻辑保留简短注释。
- Done Criteria:
  - 本地/手机环境点击 QR refresh 连续 10 次不崩溃。
  - 断网、接口失败、二维码图片下载失败、切后台/返回后旧回调都不崩溃。
  - 失败态可见且可再次刷新。
  - PostHog/logcat 有脱敏诊断证据，且无敏感字段。
  - `git diff --check` 通过。
  - `./scripts/check_api17_guardrails.sh` 通过。
  - `gradle :app:compileDebugKotlin --no-daemon` 通过。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `python scripts/check_code_health.py`（允许既有 `MainActivity` red-line 失败，但不得新增 red finding）
  - `gradle :app:compileDebugKotlin --no-daemon`
  - 触及资源/清单时执行 `gradle :app:assembleDebug --no-daemon`
- Risks:
  - 没有崩溃堆栈时，可能只能先覆盖高概率未捕获异常路径；若修后仍崩溃，需要补充 logcat 继续定位。
  - QR/session direct API 涉及网络与 crypto，不能通过吞异常假装登录成功。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-05
- Result:
  - 修复高概率崩溃点：`KugouDirectAuthClient.downloadBitmap()` 现在把 `Request.Builder().url(...)` 纳入 `runCatching`，二维码图片 URL 异常不会再抛未捕获异常杀进程。
  - `KugouAuthConfigBinder` 增加 QR refresh request generation，刷新/登出/停止后的旧异步结果不会再写 UI。
  - QR refresh 失败时进入失败/可重试状态，不启动无图轮询。
  - 新增脱敏 PostHog/runtime 事件：`kugou_qr_refresh_start/success/failed`、`kugou_qr_poll_failed`、`kugou_qr_login_success`、`kugou_session_validation_success/failed`。
  - 用户 ID runtime log 改为短 hash；不记录 token/session key/cookie/手机号/验证码/完整 URL query/auth header/API key/设备凭据。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`、`assembleDebug`；`check_code_health.py` 仍仅因既有 `MainActivity` red-line 失败。

## T-S5-OBS-120
- Task ID: `T-S5-OBS-120`
- Module ID: `M-S5-OBS-036`
- Title: S5 新功能 PostHog 覆盖补齐与敏感字段审计
- Goal: 对 S5 后续新增的 QR direct auth、酷狗内容、普通 queue、radio session、DSP direct-buffer/fail-open 等关键路径补齐低频 PostHog 或明确 runtime/logcat 证据。
- Why: 用户已确认新功能必须有足够日志；当前 PostHog 覆盖主要集中在启动、通用播放、网络门禁和更新，S5 新增功能存在观测缺口。
- Responsibility Boundary:
  - 只补低频、可聚合事件，不引入高频 progress/UI/audio-frame 噪音。
  - 事件属性只使用脱敏字段：短 ID/hash、source、stage、error_code、elapsed_ms、count、布尔状态、截断非敏感摘要。
  - 事件定义/回归清单/代码三者保持一致。
- Dependencies: `T-S5-KG-119`（建议先完成）
- Inputs:
  - `docs/POSTHOG_EVENT_DICTIONARY.md`
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
  - `app/src/main/java/com/skodamusic/app/ui/KugouAuthConfigBinder.kt`
  - `app/src/main/java/com/skodamusic/app/ui/KugouContentBinder.kt`
  - `app/src/main/java/com/skodamusic/app/playback/KugouPlaybackQueueManager.kt`
  - `app/src/main/java/com/skodamusic/app/playback/KugouRadioSessionManager.kt`
  - `app/src/main/java/com/skodamusic/app/audio/dsp/HiFiAudioProcessor.kt`
- Expected Outputs:
  - 一份 S5 observability gap list。
  - 必要的 PostHog capture 点或明确 runtime/logcat 替代证据。
  - 回归清单中的 PostHog 证据字段更新。
  - 敏感字段审计结论。
- Expected Files:
  - 视审计结果最小修改相关 feature 文件。
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
  - 必要时更新 `docs/POSTHOG_EVENT_DICTIONARY.md`。
- Files Not To Expand:
  - `app/src/main/java/com/skodamusic/app/MainActivity.kt`
- Architecture Notes:
  - 如果多个 S5 feature 需要相同脱敏属性，新增 focused helper，避免分散复制。
  - 不把 PostHog 当原始日志仓库。
- Comment Requirements:
  - 对刻意不上报的高频/敏感字段保留简短原因说明或文档说明。
- Done Criteria:
  - QR auth、Kugou 内容失败、queue/radio session 切换、DSP fail-open/bridge 至少有低频诊断证据。
  - 无敏感字段进入 PostHog/runtime logs。
  - 事件预算和 coalescing 规则不被破坏。
  - 本地校验命令通过或记录既有失败。
- Validation:
  - `git diff --check`
  - `./scripts/check_api17_guardrails.sh`
  - `python scripts/check_code_health.py`
  - `gradle :app:compileDebugKotlin --no-daemon`
- Risks:
  - 埋点过多会污染 PostHog；必须优先失败路径和状态转换，不记录每次 UI 渲染或播放进度。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes
- Status: Done 2026-06-05
- Result:
  - `KugouContentBinder` 新增低频 PostHog 事件：`kugou_content_load_success/failed`、`kugou_queue_start`、`kugou_radio_session_start`。
  - `PostHogTracker` 敏感 key 过滤扩展到 session key、cookie、phone/mobile、verification code、auth header、API key、query、credential、secret、payload 等。
  - 新增 `docs/S5_OBSERVABILITY_COVERAGE.md`，明确 QR/content/queue/radio/DSP 的证据路径；DSP 热路径继续使用 runtime/logcat，不接入高频 PostHog。
  - `docs/POSTHOG_EVENT_DICTIONARY.md` 与 `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md` 已同步事件与验收项。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`、`assembleDebug`；`check_code_health.py` 仍仅因既有 `MainActivity` red-line 失败。

## T-S5-PLAY-110
- Task ID: `T-S5-PLAY-110`
- Module ID: `M-S5-PLAY-031`
- Title: 纯酷狗播放状态边界，切断 Emby 队列叠加
- Goal: 引入 source playback session，让酷狗播放的 Now Playing、service state、next/previous 不再依赖 Emby `loadedTracks/currentTrackIndex`。
- Why: 当前酷狗播放是单曲直连，但 UI/service/next 仍读 Emby 队列，导致“Emby 叠加播放，没有切换到纯酷狗”。
- Dependencies: `T-S5-KG-118`
- Inputs:
  - `MainActivity.kt` 中 `playKugouTrack`、`prepareAndPlayKugouUrl`、`reportPlaybackStateToService`、next/previous、render 相关逻辑。
  - `PlaybackService` state contract。
  - `MainModels.kt` source models。
- Expected Outputs:
  - 当前播放 session 能区分 Emby / KugouSong / KugouRadio。
  - 酷狗播放时 Now Playing title/artist/duration 来自 `SourceTrack`。
  - 酷狗模式不触发 Emby resume/auto refresh/tail refill。
  - next/previous 按 session 类型分流。
- Done Criteria:
  - 酷狗播放时 service `hasTrack/trackId/title/duration` 不从 Emby 队列取值。
  - Emby 队列在酷狗播放时不被自动推进。
  - Emby 来源仍可独立播放。
- Risks:
  - 后台方向盘按键依赖 service state，字段变更需保持兼容。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-PLAY-111
- Task ID: `T-S5-PLAY-111`
- Module ID: `M-S5-PLAY-032`
- Title: Kugou 普通歌曲队列按 .NET PlaybackQueueManager 实现
- Goal: 为推荐歌曲和发现歌单歌曲建立独立酷狗普通歌曲队列，行为参考 `.NET` `PlaybackQueueManager`。
- Why: 酷狗队列和 Emby 队列不同；当前 Android 未建立酷狗 queue，只是点击单曲播放。
- Dependencies: `T-S5-PLAY-110`
- Inputs:
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/Services/PlaybackQueueManager.cs`
  - 推荐歌曲上下文列表 `kugouRecommendedTracks`
  - 发现歌单歌曲上下文列表 `kugouDiscoverSongs`
  - 当前 source playback session。
- Expected Outputs:
  - `KugouQueueManager` 或等价类。
  - `setupQueue(song, contextList)`，列表超过 300 时围绕当前歌曲截取。
  - next/previous 循环。
  - 队列页显示酷狗普通队列。
- Done Criteria:
  - 点击推荐歌曲/发现歌单歌曲后，next/previous 在同一上下文内循环。
  - 不改动 Emby `loadedTracks` 作为酷狗队列存储。
  - 队列页能看出当前是酷狗普通队列。
- Risks:
  - `SourceTrack` equality 需要使用稳定 identity，不能依赖对象引用。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes

## T-S5-PLAY-112
- Task ID: `T-S5-PLAY-112`
- Module ID: `M-S5-PLAY-032`
- Title: Kugou Radio/FM session 按 .NET PersonalFmService 实现
- Goal: 将推荐电台播放改为独立 radio/FM session，同一电台持续播放，下一曲由 session 内部推进。
- Why: 用户明确补充电台不是普通歌曲队列；当前 Android 把电台歌曲当 `SourceTrack` 普通点击播放，语义错误。
- Dependencies: `T-S5-PLAY-110`
- Inputs:
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/Services/PersonalFmService.cs`
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/ViewModels/PlayerViewModel.PersonalFm.cs`
  - `KugouMusic.NET/src/Apps/KugouAvaloniaPlayer/ViewModels/PlayerViewModel.Queue.cs`
  - Android `requestKugouRecommendedRadios` / `requestKugouRadioSongs`
- Expected Outputs:
  - `KugouRadioSessionManager` 或等价类。
  - active session/current/upcoming/history 最小状态。
  - radio active 时 next/previous 分流到 radio session。
  - 队列页显示 radio session 状态。
- Done Criteria:
  - 点击电台进入 radio session。
  - radio active 时下一曲不走普通酷狗队列和 Emby 队列。
  - 同一电台持续播放，直到用户切换来源/清空/退出 session。
- Risks:
  - `.NET` Personal FM 的 action/report 可能依赖接口；本轮若 Android 缺失对应 direct API，需标记缺口，不猜。
- Size: M
- Execution Mode: Module
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 新增 `KugouRadioSessionManager.kt`，维护 radio active session、current/upcoming/history。
  - `KugouContentBinder` 的 radio 歌曲点击和电台加载成功回调改为启动 radio session；普通 Kugou queue 与 radio session 互斥。
  - `MainActivity` 的 Kugou prev/next/completion 分流优先走 radio session，radio active 时不落到普通 queue 或 Emby queue。
  - 队列页可显示酷狗电台队列（current + upcoming）。
  - 依据 `.NET PersonalFmService`/`PlayerViewModel.PersonalFm.cs` 的 session/advance/previous/queue 分流；Android 端未猜测 direct Personal FM raw endpoint，仍基于当前已加载的 `/fm/songs` 列表形成最小 session。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`、`assembleDebug`；`check_code_health.py` 仍因既有 `MainActivity` red-line 失败，blocking finding 数为 2。

## T-S4-AUDIO-097
- Task ID: `T-S4-AUDIO-097`
- Module ID: `M-S4-AUDIO-023`
- Title: DSP 播放按钮持续红框诊断与修正
- Goal: 修正 DSP runtime 状态持续红色问题，让红色只代表真实 fail-open/bypass/error。
- Why: 用户反馈现在音效持续红色边框，无法确认 native DSP 正常工作。
- Dependencies: `T-S4-AUDIO-096`
- Inputs:
  - `app/src/main/java/com/skodamusic/app/audio/dsp/HiFiDspController.kt`
  - `app/src/main/java/com/skodamusic/app/audio/dsp/HiFiAudioProcessor.kt`
  - `app/src/main/cpp/native_hifi_dsp.cpp`
  - `MainActivity` DSP state rendering。
- Expected Outputs:
  - 状态原因诊断：native-not-ready、non-direct-buffer、native-process-error、unsupported format、native bypass 等。
  - 正常 active/degraded/fail-open/disabled 映射修正。
  - 必要时修正 native 永久 bypass 后的复位或 yellow/red 分层。
- Done Criteria:
  - 正常 native active 不持续红色。
  - 降级/超预算为黄色。
  - fail-open/bypass/error 为红色且日志有原因。
  - 关闭/原声/release 后回灰。
- Risks:
  - 如果红色来自真实 AC83xx native 性能超预算，可能需要后续实机调参；本轮至少要给出明确原因。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 诊断出持续红框的高概率误报路径：`HiFiAudioProcessor` 遇到 API17/ExoPlayer heap `ByteBuffer` 时直接走 `non-direct-buffer` bypass，并发布 `FAIL_OPEN`。
  - 修正为 direct scratch bridge：heap input/output buffer 会复制到 direct scratch buffer 后继续调用 native DSP，不再直接把按钮置红。
  - 保留真实 fail-open 红色路径：unsupported format、native-not-ready、native-process-error、native `STATUS_BYPASS/STATUS_ERROR` 或 error/bypass flags。
  - 新日志 `hifi-dsp native direct-buffer bridge input=<...> output=<...>` 可用于确认是否发生 heap/direct 桥接。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`、`assembleDebug`；`check_code_health.py` 仍因既有 `MainActivity` red-line 失败，blocking finding 数为 2。

## T-S5-VAL-113
- Task ID: `T-S5-VAL-113`
- Module ID: `M-S5-VAL-033`
- Title: S5 纠偏 API17 回归清单与本地验证
- Goal: 更新验证清单并执行本轮构建/guardrails。
- Why: 本轮触及架构拆分、播放状态、队列、电台和 DSP，必须闭环验证。
- Dependencies: `T-S5-PLAY-111`, `T-S5-PLAY-112`, `T-S4-AUDIO-097`
- Inputs:
  - `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`
  - 本轮代码改动。
- Expected Outputs:
  - 回归清单覆盖 MainActivity 拆分、纯酷狗、普通队列、电台 session、DSP 状态。
  - 本地验证结果写入 context。
- Done Criteria:
  - `git diff --check` 通过。
  - `./scripts/check_api17_guardrails.sh` 通过。
  - `gradle :app:compileDebugKotlin --no-daemon` 通过。
  - 触及资源/播放/native 时 `gradle :app:assembleDebug --no-daemon` 通过。
- Risks:
  - 真实酷狗登录/电台验证仍依赖外部网络与账号环境。
- Size: S
- Execution Mode: Single
- Minimal Loop: Yes
- Status: Done 2026-06-04
- Result:
  - 更新 `docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`，Scope 增加 `T-S5-VAL-113`。
  - I 组补充 DSP 边框颜色语义与 direct-buffer bridge 验证项。
  - K 组更新为当前 Kugou 内容页真实口径。
  - 新增 M 组：纯酷狗播放、普通 queue、Radio session。
  - 新增 N 组：MainActivity split / page shell decision。
  - 追加 T-S5-VAL-113 本地验证快照。
  - 本地验证通过 `git diff --check`、API17 guardrails、`compileDebugKotlin`、`assembleDebug`；`check_code_health.py` 仍因既有 `MainActivity` red-line 失败，blocking finding 数为 2。

## T-S5-VIP-133
- Task ID: `T-S5-VIP-133`
- Module ID: `M-S5-VIP-040`
- Title: VIP direct user client 与记录/领取/升级解析
- Status: Done 2026-06-07
- Result:
  - 新增 `KugouDirectUserClient`，按 `.NET` `RawUserApi` 接入 record/receive/upgrade 三个接口。
  - 解析 `day/receive_vip/vip_type/status/error_code` 最小字段，成功 upgrade 后写回 session `vipType`。

## T-S5-VIP-134
- Task ID: `T-S5-VIP-134`
- Module ID: `M-S5-VIP-040`
- Title: 每日 VIP coordinator、本地兜底记录与失败重试
- Status: Done 2026-06-07
- Result:
  - 新增 `KugouDailyVipCoordinator`。
  - 冷启动 cached session 和扫码登录成功后触发。
  - 服务端 record 优先；record 不可用时本地账号+日期 fallback，并继续尝试 receive/upgrade。
  - 失败重试有每日次数、冷却和退避，不阻塞启动/登录/每日推荐/播放。

## T-S5-PLAY-135
- Task ID: `T-S5-PLAY-135`
- Module ID: `M-S5-VIP-040`
- Title: 播放 URL 失败分类与无权限/VIP UI 提示
- Status: Done 2026-06-07
- Result:
  - `/v5/url` direct 返回扩展为 `KugouPlayUrlResult`。
  - 分类 `VIP_REQUIRED/PERMISSION_DENIED/PAID_REQUIRED/TRIAL_UNAVAILABLE/NETWORK/SESSION_REQUIRED/UNKNOWN`。
  - 无权限/VIP/付费类失败显示明确“无权限播放，可能需要 VIP”。

## T-S5-OBS-136
- Task ID: `T-S5-OBS-136`
- Module ID: `M-S5-VIP-040`
- Title: VIP/权限失败观测、PostHog 查询限制记录与 API17 回归清单
- Status: Done 2026-06-07
- Result:
  - 新增 `kugou_daily_vip_start/success/failed`。
  - `kugou_direct_play_url_failed` 增加 `failure_kind/error_code/http_code/kg_status/priv_status/err_code`。
  - 更新 `docs/S5_OBSERVABILITY_COVERAGE.md`、`docs/POSTHOG_EVENT_DICTIONARY.md`、`docs/API17_INTERACTION_REGRESSION_CHECKLIST.md`。
  - 记录当前无 PostHog personal/query token，无法直接查询事件流。

## Blocked

### B-KG-EMBY-INGEST-001
- Task ID: `B-KG-EMBY-INGEST-001`
- Title: 点赞后将播放缓存上传到 Emby 并纳入媒体库
- Status: Blocked / Deferred
- Blocked By:
  - Emby 是否支持 API 上传音频并入库未确认。
  - 若不支持，需要确认服务端代理写入媒体目录 + 触发扫描方案。
  - 100MB 缓存上限与网络空闲重试策略需后续需求确认。
- Current Requirement:
  - 当前只记录点赞状态和入库阻塞状态，不执行上传。
